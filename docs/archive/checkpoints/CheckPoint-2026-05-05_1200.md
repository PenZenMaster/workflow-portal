## Context Summary

Local dev environment setup and first feature iteration for the Workflow Portal.
Stack: Express 5 + React 18 + SQLite (Drizzle ORM) + TypeScript + Vite.
Deployed to cPanel at portal.fullmetaljacketseo.com.

## Accomplishments This Session

- Cloned repo from GitHub, initialized proper git tracking
- Fixed Windows incompatibilities (cross-env, reusePort, .gitattributes)
- Added full testing infrastructure (vitest 3.x, @testing-library/react, supertest)
- Added CLAUDE.md with Project Start / Checkpoint / Shutdown procedures
- Added Node.js standards reference (docs/standards/nodejs.md)
- Built LaunchInputsDialog: collecting required inputs before launching AI skill session
- Fixed db persistence: DATA_DB_PATH + SESSION_DB_PATH env vars keep databases outside app root
- Added semantic versioning: v0.1.0 badge fixed bottom-right on all screens
- Fixed missing @types declaration for better-sqlite3-session-store
- Deployed build to cPanel server successfully

## Technical Changes

| File | Change |
|------|--------|
| package.json | version 0.1.0, cross-env, test scripts, testing devDeps |
| server/index.ts | reusePort guarded by process.platform check |
| server/storage.ts | DATA_DB_PATH env var for portable db path |
| server/types.d.ts | Missing type declaration shim for better-sqlite3-session-store |
| client/src/components/LaunchInputsDialog.tsx | New — input collection dialog before AI launch |
| client/src/components/WorkflowCard.tsx | Launch button opens dialog when inputs present |
| client/src/App.tsx | Version badge on all screens |
| client/src/env.d.ts | __APP_VERSION__ global declaration |
| vite.config.ts | define __APP_VERSION__ from package.json |
| vitest.config.ts | workspace projects (client/server), define __APP_VERSION__ |
| .gitattributes | LF line endings for server, CRLF for .cmd |
| .env.example | DATA_DB_PATH + SESSION_DB_PATH documented |
| docs/standards/nodejs.md | Node.js dev standards reference |
| CLAUDE.md | Project procedures |

## Known Issues / Blockers

- Server cannot run locally on Windows (ENOTSUP on socket bind) — dev testing via cPanel deploy only
- DATA_DB_PATH and SESSION_DB_PATH env vars need to be set in cPanel before first account creation
- User was prompted to create admin account again after latest deploy (env vars not yet set)

## Next Session Priorities

1. Confirm DATA_DB_PATH/SESSION_DB_PATH are set on cPanel and admin account persists across deploys
2. Begin DEV plan item #1 verification: test LaunchInputsDialog end-to-end on live server
3. Plan and implement next feature requests

## Git Status

Branch: main
Last commit: 315a9e2 feat(version): add semantic versioning and version badge on all pages
Remote: up to date with origin/main
